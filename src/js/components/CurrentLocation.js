var React = require('react');

module.exports = React.createClass({

	render() {
		return (
			<section id="current-location">
				<header>Current Location</header>
				<article>Paris, France</article>
			</section>
		);
	}
});
